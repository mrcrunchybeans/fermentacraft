## Fixed in Next Release

The standalone EXE was missing plugin DLLs. We now publish two versions:

**Download one of these:**
- `fermentacraft-X.Y.Z.msix` - Recommended. Right-click → Install
- `fermentacraft-X.Y.Z-portable.zip` - Extract and run. No installation needed

Both include all required DLLs. The issue won't occur with either version.
